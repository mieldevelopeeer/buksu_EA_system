import React from "react";
import ProgramHeadEnrollment from "@/Pages/ProgramHead/Evaluation/Enrollment";

export default function FacultyEnrollment(props) {
  return <ProgramHeadEnrollment {...props} layoutOverride="faculty" />;
}
